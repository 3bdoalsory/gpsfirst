# GPS Platform V9.6.1

Fix pack on top of V9.6:
- Reliable return from admin "view as client" session.
- Improved view-as-admin banner layout.
- Fleet summary cards restyled as compact dark-blue boxes.
- Vehicle map visibility eye uses eye + X state instead of animated/alternate symbol.
- Admin vehicle notes removed from new-device registration and moved to a dedicated dark admin-only notes section in device editing.
- Account action buttons laid out without overlap.
- Added confirmed client-account deletion: keeps devices registered, unassigns them, and clears their old GPS/customer-associated data.

Database file is intentionally not included.
